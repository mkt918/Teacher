# 📋 パッチノート

---

## 🆙 v1.5.0 — "Full Sync & Report Update"

### ✨ 新機能

**☁️ クラウド同期の対象を全データに拡大**
- これまで同期対象はToDo・週間スケジュール・年間行事の3つだけだったが、
  生徒名簿・出欠記録・個人メモ・座席表・バス座席・グループ分け・
  クラス係・保護者会・テスト問題・要録所見を含む**アプリのほぼ全データ**が
  複数端末で同期されるように（端末ローカルの基本設定のみ対象外）
- Google Sheetsのセル上限（50,000文字）を超える大きなデータも
  複数セルへの分割保存で安全に扱えるように
- **要対応**：既にGASを設定済みの場合、`gas/Code.gs` の最新版への
  更新（新バージョンとして再デプロイ）が必要。手順は
  [docs/GAS_SETUP_GUIDE.md](docs/GAS_SETUP_GUIDE.md) の
  「GASスクリプトを新しいバージョンに更新する」を参照。
  更新するまでは従来どおりToDo等3種のみ同期される（データは壊れない）

**📄 要録所見ツールを実装（「準備中」から昇格）**
- 生徒一覧（入力済み✅/未入力の進捗表示つき）から選んで所見を入力
- 入力すると自動保存。文字数カウントと目安文字数（初期値300字、変更可）の
  超過表示つき
- 「📤 全所見を出力」で全生徒の所見をテキストファイルに一括出力

### 🐛 修正・改善

**👥 生徒名簿の改善（v1.4.2以降の分をまとめて記載）**
- 生徒編集の必須項目を出席番号のみに変更（名前は後から入力できる）
- 一覧を縦5人×2列＝10人のブロック単位で横に並ぶコンパクト表示に刷新
- 5人区切りの列の間に余白を追加し、視覚的に把握しやすく

**📊 欠席一覧クロス表の調整**
- フォントを縮小し、全セルを折り返しなしの1行表示に固定

**ℹ️ その他**
- 設定ページの「アプリ情報」のバージョン表記が「v1.0」のまま
  固定されていたのを修正（実バージョンを自動表示）
- スモークテストを9件→13件に拡充（要注意フラグの保存、年度判定、
  所見の保存/削除、クラウド同期ペイロードの回帰テストを追加）

---

## 🆙 v1.4.2 — "Safe Distribution Update"

他の先生にもこのアプリを配布できるように、クラウド同期の仕組みを
「開発者専用」から「各自が自分のGASを登録する」方式に変更した更新。

### 🐛 重要な修正（要対応）

**☁️ クラウド同期URLが開発者個人のものに固定されていた不具合を修正**
- これまでGASのデプロイURLがソースコードに直書きされており、
  設定画面でURLを変更して保存しても、**次回アプリを開き直すと
  常に開発者個人のスプレッドシートへ同期される**状態になっていた
  （設定を保存した直後のセッションでしか反映されない不具合）
- この修正により、「⚙️ 基本設定」に保存したURLが常に使われるように
  なった。**これまでクラウド同期を使っていた方は、この更新後に一度
  「⚙️ 基本設定」を開いてGASエンドポイントURLを再入力し、
  「設定を保存する」を押し直してください**（再入力しないと
  クラウド同期が無効の状態になります）
- 配布用の初回セットアップ手順を [docs/GAS_SETUP_GUIDE.md](docs/GAS_SETUP_GUIDE.md)
  にまとめて追加

**📅 年間行事予定がクラウド同期の対象から漏れていた不具合を修正**
- アプリ側は`calendar`というフィールド名で送信していたのに対し、
  GAS側は`events`という別名で受信しており、送っても保存されず、
  読み込んでも反映されない状態だった。GAS側のフィールド名を
  `calendar`に統一

---

## 🆙 v1.4.1 — "Attendance Overhaul"

### ✨ 新機能

**📊 出欠サマリー — アコーディオン全面刷新**
- 生徒全員を折りたたみ式の「生徒一覧を表示」アコーディオンに収納するように変更
  （デフォルトは全員非表示、クリックで一覧展開）
- 各生徒に⚠️ボタンを追加。**手動で「要注意」フラグを付けた生徒は、
  アコーディオンの外（常に見える専用エリア）に固定表示**されるように
- フラグの状態は保存され、次回起動時も引き継がれる

**📋 欠席一覧モーダル — 印刷・ソート・クロス表に対応**
- 「欠席一覧をすべて見る」モーダルに🖨️印刷ボタンを追加
- 一覧表示を「日付順」「科目順」で切り替え可能に
- **新規**：「科目別クロス表」ビューを追加。横に科目、縦に欠席回数（1回目・2回目…）
  を並べ、交差するセルにその日付・曜日・時限を表示。一覧表・クロス表どちらも
  個別に印刷可能

### 🐛 修正

**🎨 ヘッダーの年度バッジが見えない不具合を修正**
- ヘッダー右上の年度バッジ（濃い青背景）の文字色が、`.app-title`の
  グラデーション文字効果（`-webkit-text-fill-color: transparent`）を
  継承してしまい、白文字のはずが透明になって読めなくなっていた不具合を修正

---

## 🆙 v1.4.0 — "Stability & Accessibility Update"

コードレビューでランク付けを行い、指摘事項をすべて解消したメンテナンスアップデート。見た目の変化は少ないが、土台の健全性が大きく向上。

### ✨ 新機能

**📅 年度管理**
- 「各種設定」に年度セレクトを追加（前後5年分から選択可能）
- ヘッダーに現在の年度を常時表示（濃い青バッジ、視認性を確保）
- 出欠管理・年間行事の集計期間が、設定した年度に自動連動するように

**🔒 全データ削除の保護を強化**
- 従来はconfirm()を2回クリックするだけだった「全データ削除」を、
  「削除」と文字入力しないと実行できないtype-to-confirmモーダルに変更

**🗂️ 履歴UIの統一**
- バス座席・グループ分け・クラス係の履歴機能を、prompt()で番号を
  手入力する旧式UIから、席替え・保護者会と同じモーダルUI
  （保存名入力→一覧から復元/削除）に統一
- 3機能で個別に実装されていたコードを共通部品
  `HistoryModal`（`js/utils/history-modal.js`）に統合し重複を解消

### ♿ アクセシビリティ

- 記号のみのアイコンボタン（✕/🔒/🔓/🗑️/×/±など40箇所以上）に
  aria-labelを追加。スクリーンリーダーが記号そのものを読み上げる
  問題を解消
- 全モーダル（16箇所）に`role="dialog"` `aria-modal="true"`を追加
- キーボード操作時のフォーカス表示をサイト全体に統一して追加

### 🎨 内部の大掃除

- `style.css`と`modern-ui.css`で同じセレクタが二重・三重に定義され、
  読み込み順に暗黙的に依存していた73箇所を、実際の見た目を変えずに
  1セレクタ1宣言へ統合（postcssのASTで機械的に計算・検証）
- ページ内で丸ごと複製されていた設定フォーム（21個のID重複）を削除。
  席替えで実際に事故になったのと同じ「たまたま動いていただけ」の
  危険な状態を解消
- 保護者会の到達不能コード（存在しないボタンに紐づいていた印刷処理）
  を削除

### ✅ テスト基盤（新規導入）

- これまでテスト・リンタが一切なかった状態を解消。ブラウザで開くだけの
  軽量スモークテスト9件を追加し、GitHub Actionsで push・PR毎に自動実行
- テスト対象は、このセッションで実際に発生した「保存で他データが消える」
  「エスケープ漏れ」「履歴の復元対象がずれる」といったバグ全種の回帰検知

### 🐛 その他の修正

- クラス係のprint出力で生徒番号・氏名・係名がエスケープされずHTML注入
  可能だった箇所を修正
- クラス係の確認・入力ダイアログの二重エスケープ改行バグを修正

---

## 🆙 v1.3.0 — "Redesign Update"

### ✨ 新機能・追加コンテンツ

**☁️ クラウド同期システム 実装**
- GAS（Google Apps Script）連携を標準搭載。設定不要で起動時から常時同期がオンに
- ToDoリスト・年間行事計画も同期対象に追加

**🪑 席替えツール — 全面リメイク**
- ステップガイド「①形を決める→②割り当てる→③保存・印刷」を新規実装（初回は自動表示）
- モード選択UIをカード型に刷新
- 🔒/🔓ロックアイコンの意味をチュートリアルで明示

**🚌 バス座席表 — グラフィック刷新**
- フロントガラス・運転席・乗降口・タイヤを追加し、実車風の見た目に大幅アップグレード
- **NEW**: 空席ロック／生徒ロック機能を実装（🔓/🔒）
- 隠しコマンド状態だった「バス削除」ボタンを解禁

**📅 保護者会時間決定ツール — 全面リメイク**
- ステップガイド「①日程設定→②希望収集→③自動配置・確認→④保存・印刷」を実装
- 未割り当てリストに進捗表示「希望入力済み：X/Y人」を追加
- UIをユニバーサルデザイン仕様に強化（全ボタン最小44px、文字拡大、色＋✓マークの二重表示）

**📊 時間割・出欠管理 — 機能拡張**
- 出欠サマリーの科目名クリックで「授業日一覧モーダル」が開くように（統計カードは期間指定と連動、回数列つき）
- 時間割設定の科目カードに✏️名前変更ボタンを追加（変更は時間割・当日変更履歴に自動遡及）
- 授業担当統計の集計開始日を「今年度4月1日」にデフォルト固定
- 時間割印刷に区切り太線を追加
- **生徒別出欠モーダルを全面刷新**：カレンダー形式 → 日付×時限の一覧表形式に変更
  - 表内に科目名を常時表示
  - セルクリックで連続確定（別モーダル遷移が不要に）
  - 「全欠席」ワンクリックボタンを追加

**✏️ テスト問題補助ツール — 機能追加**
- 問題一覧の表示順を「No順／ランダム順」でトグル可能に
- 解答コピーを「記号」「言葉」「両方」の3モードに対応

**👥 生徒名簿 — QoL追加**
- 「🔢 番号で一括登録」を実装。開始番号と人数を指定するだけで一括登録可能に

**🗑️ コンテンツ削除**
- 未使用だった「ファイル管理」「マニュアル」機能を削除（軽量化）

---

### ⚖️ バランス調整

**保護者会：自動配置アルゴリズムを調整**
- 旧アルゴリズムは初期状態でのみ「希望が少ない順」を計算しており、割り当てが進むにつれて優先順位が古くなる問題があった
- 1人確定するたびに残り全員の「現在選べる希望枠数」を再計算する方式に変更
- 結果：希望が少ない（制約が厳しい）生徒がより優先され、デッドロック（希望全滅）が発生しにくくなった

---

### 🐛 不具合修正

**🔴 Critical（データ消失系）**
- バス座席：座席操作のたびに保存履歴が消滅するバグを修正
- 保護者会：①履歴保存時に生徒の希望時間が保存されず、復元すると消失／②「全クリア」が説明文に反して希望時間・履歴まで削除　の2件を修正
- グループ分け：グループ編集のたびに保存履歴が消滅するバグを修正
- 出欠サマリー：再描画後に科目名クリックが反応しなくなる不具合を修正

**🟡 表示バグ**
- 席替え・保護者会・クラス係：確認ダイアログの改行が効かず、`\n`という文字列がそのまま表示される二重エスケープバグを一括修正

**🟢 その他**
- 席替え：「保存／読込」ボタンが古いモーダルの残骸と衝突し、クリックしても無反応だった不具合を修正
- バス座席：「バス削除」ボタンがHTML未実装で一度も動作しなかった不具合を修正
- CI：Node.js 20 非推奨警告を解消

---

### 🔧 内部改善・技術的負債の解消

- 年間行事予定・時間割の保存処理を「オブジェクト全置換方式」から「個別フィールド上書き方式」に変更
  - 今後これらの機能に新フィールドが追加されても、保存のたびに消える事故が起きないよう予防
- 未使用の死んだコード（旧履歴実装、静的HTML残骸、参照されないボタンハンドラ等）を一括整理

---

## 📌 過去のアップデート（v1.2 以前）

- モバイル対応ハンバーガーメニューを追加
- 時間割印刷に来週・再来週のメモ欄行を追加、レイアウト微調整を多数実施
- 科目名変更を時間割・日々の変更履歴に遡及適用
